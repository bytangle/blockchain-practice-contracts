// SPDX-License-Identifier: GPL-3.0

pragma solidity >= 0.8.0 < 0.9.0;

/**
 * @title Counter - basic counter
 * @author Edicha Joshua
 */
contract Counter {
    mapping(address => uint) private _values;
}